"use client";

type Props = {
    initialPercentage: number;
    initialHearts: number;
}


export const Quiz = ({initialPercentage, initialHearts}: Props) => {
    return (
        <div>
            
        </div>
    )
}

