type Props = {
    activeCourse: any; 
    hearts: number;
    points: number;
}

export const UserProgress = ( { activeCourse, hearts, points }: Props) => {
    return (
        <div>
            User Progress
        </div>
    );
};
